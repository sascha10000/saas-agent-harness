services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=sqlite://data/app.sqlite
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Postgres upgrade path — enable only if project/STACK.md justifies the deviation,
  # then set DATABASE_URL=postgres://app:app@db:5432/app and add the sqlx "postgres"
  # feature in Cargo.toml.
  # db:
  #   image: postgres:17-alpine
  #   environment:
  #     - POSTGRES_USER=app
  #     - POSTGRES_PASSWORD=app
  #     - POSTGRES_DB=app
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   pgdata:
